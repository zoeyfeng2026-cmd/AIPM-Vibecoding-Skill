# 03 · 数据与接口（Data & Interfaces）

> **定义**：数据模型、实体间关系、API 接口定义。
> **为什么必须有**：数据模型是产品骨架，接口是模块间契约。定义清楚了，剩下的都是围绕数据的操作。

---

## 1. 实体关系

<!-- 规则：用文字或 ASCII/ Mermaid 图描述实体之间的关系 -->
<!-- 好的写法：users ←→ projects 通过 project_members 多对多关联 -->
<!-- 差的写法：有关联 -->

```
（在这里画 ER 图或文字描述关系）
```

## 2. 实体定义

<!-- 规则：每个实体列出所有字段，每个字段包含：字段名、类型、说明 -->
<!-- 好的写法：见下方表格 -->
<!-- 差的写法：有 id 和 name -->

### 实体 A：（填写实体名）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | TEXT PK | `前缀-${uuid}` |
| （填写） | （填写） | （填写） |

### 实体 B：（填写实体名）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | TEXT PK | `前缀-${uuid}` |
| （填写） | （填写） | （填写） |

## 3. ID 命名规则

<!-- 规则：给每个实体定义统一的 ID 前缀，方便调试和区分 -->
<!-- 好的写法：proj-${uuid}, member-${uuid}, task-${uuid} -->
<!-- 差的写法：不写（AI 可能用不同的命名方式） -->

- （实体名）：`（前缀）-${uuid}`
- （实体名）：`（前缀）-${uuid}`

## 4. API 接口

<!-- 规则：每个接口定义：方法、路径、请求体、响应体 -->
<!-- 好的写法：见下方表格 -->
<!-- 差的写法：有个列表接口、有个创建接口 -->

### （模块名）

```
GET    /api/（路径）        → 响应体描述
POST   /api/（路径）        请求体: {字段} → 响应体描述
PUT    /api/（路径）/:id    请求体: {字段} → 响应体描述
DELETE /api/（路径）/:id    → 204
```

## 5. 流式接口（如有）

<!-- 规则：定义事件类型、每个事件的数据格式、事件顺序 -->
<!-- 好的写法：SSE events: message_start {messageId, agentId} → content_delta {messageId, content}* → message_end {messageId} → done -->
<!-- 差的写法：流式返回 -->

```
事件类型：SSE / WebSocket / 其他
事件序列：
  event: （事件名）    {数据格式}
  event: （事件名）    {数据格式}
  ...
```
