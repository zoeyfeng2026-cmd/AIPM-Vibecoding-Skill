# 05 · 验证与边界（Verification & Boundaries）

> **定义**：成功标准、测试流程、兜底策略、性能限制、不做什么。
> **为什么必须有**：AI 会省略错误处理和边界条件。这一章强制 AI 在写代码时同时写兜底逻辑。

---

## 1. 成功标准

<!-- 规则：一条可执行的端到端流程，按步骤写，每步有明确的预期结果 -->
<!-- 好的写法：
1. 运行 `npm run dev`，浏览器打开 http://localhost:3000 → 看到首页
2. 点击「+ 新建」→ 弹出表单
3. 填写必填字段，点击保存 → 新数据出现在列表中
4. 点击某条数据 → 进入详情页，显示完整信息
5. 刷新页面 → 数据还在
-->
<!-- 差的写法：能用就行 -->

1. （操作）→ （预期结果）
2. （操作）→ （预期结果）
3. ...

## 2. 兜底策略

<!-- 规则：列出所有外部依赖（AI、第三方 API、文件系统等），每个写出失败时的处理方式 -->
<!-- 好的写法：见下方表格 -->
<!-- 差的写法：出错就报错 -->

| 外部依赖 | 失败场景 | 处理方式 |
|----------|---------|---------|
| （填写） | 超时（>30s） | （填写） |
| （填写） | API Key 无效 | （填写） |
| （填写） | 服务不可达 | （填写） |
| （填写） | 无结果 | （填写） |

## 3. 性能限制

<!-- 规则：给出具体数字，不要模糊描述 -->
<!-- 好的写法：单条最大 10000 字符，文件最大 10MB，列表最多 1000 条 -->
<!-- 差的写法：不要太慢 -->

| 限制项 | 值 | 说明 |
|--------|------|------|
| （填写） | （填写） | （填写） |
| （填写） | （填写） | （填写） |

## 4. V1 不做什么

<!-- 规则：与模块 1 的"不做什么"互补，这里更具体到版本层面 -->
<!-- 好的写法：V1 不做导出、不做暗色模式、不做多用户、不做支付 -->
<!-- 差的写法：不写（AI 可能自己加功能） -->

- （填写）
- （填写）
- （填写）

## 5. 降级方案

<!-- 规则：核心功能的依赖不可用时，产品如何降级而不是完全不可用 -->
<!-- 好的写法：AI 推荐服务不可用 → 推荐按钮置灰 + 显示"推荐暂不可用"，其他功能正常 -->
<!-- 差的写法：不写 |

| 核心功能 | 依赖不可用时 | 降级表现 |
|----------|------------|---------|
| （填写） | （填写） | （填写） |
| （填写） | （填写） | （填写） |
